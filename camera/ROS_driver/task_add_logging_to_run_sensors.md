# Context
Filename: task_add_logging_to_run_sensors.md
Created On: 2025-11-10
Created By: AI Assistant
Associated Protocol: RIPER-5 + Multidimensional + Agent Protocol

# Task Description
Add comprehensive logging to the `run_sensors` script so that all console output from each launched sensor process is saved to disk while maintaining the current gnome-terminal tab display functionality.

# Project Overview
The ISDS (Intelligent Sensor Data System) workspace uses a bash script (`run_sensors`) to launch multiple ROS sensor nodes in separate gnome-terminal tabs. Currently, all output only displays in the terminals and is lost when tabs are closed or the system restarts. The client requires persistent logging of all sensor outputs to disk for debugging and monitoring purposes.

---
*The following sections are maintained by the AI during protocol execution*
---

# Analysis (Populated by RESEARCH mode)
## Current State:
- **Script Location**: `/home/kodifly/isds_ws/src/run_sensors`
- **7 Processes Launched**:
  1. Sensor Recorder (roslaunch sensor_record sensor_recorder.launch)
  2. 1m Snapshot/Distance Trigger (roslaunch distance_trigger trigger.launch)
  3. Camera (roslaunch mvs_ros_driver mvs_multi_cam.launch)
  4. LiDAR (roslaunch ouster_ros driver.launch)
  5. GPS (roslaunch gps_rtk rtk.launch)
  6. RTSP Streamer (roslaunch rtsp_image_transport stream_camera.launch)
  7. Stream Forwarder (./stream_forwarder.sh)

## Current Logging Infrastructure:
- `/home/kodifly/logs/` directory exists with date/time folder structure (YYYYMMDD/HHMMSS/)
- `start_sensors.py` (alternative script) uses this structure for metrics logging
- No console output logging currently implemented in `run_sensors`

## Key Constraints:
- Must maintain gnome-terminal tab visibility for real-time monitoring
- Should use existing `/home/kodifly/logs/` directory structure
- Minimal changes to existing script
- All processes run within ROS workspace context

# Proposed Solution (Populated by INNOVATE mode)
## Selected Approach: Option 2 - `tee` Command Integration

### Why This Solution:
- Maintains current UI/UX (terminal tabs remain visible)
- Simple implementation (just modify commands with pipe to `tee`)
- No additional scripts needed
- Automatic timestamping via directory structure
- Consistent with existing logging infrastructure

### Log Structure:
```
/home/kodifly/logs/run_sensors/YYYYMMDD/HHMMSS/
  ├── sensor_recorder.log
  ├── distance_trigger.log
  ├── camera.log
  ├── lidar.log
  ├── gps.log
  ├── rtsp_streamer.log
  └── stream_forwarder.log
```

### Technical Approach:
1. Add log directory creation at script start
2. Generate timestamp-based directory path
3. Modify each gnome-terminal tab command to pipe through `tee -a`
4. Use `2>&1` to capture both stdout and stderr

# Implementation Plan (Generated by PLAN mode)

## Change Plan:
- **File**: `/home/kodifly/isds_ws/src/run_sensors`
- **Rationale**: Add logging infrastructure while preserving existing functionality
- **Approach**: Insert log directory setup at beginning, then modify each command to use `tee`

## Detailed Modifications:

### 1. Add Log Directory Setup (before gnome-terminal command)
- Create timestamp variables (date: YYYYMMDD, time: HHMMSS)
- Define LOG_BASE_DIR variable
- Create log directory structure using `mkdir -p`
- Define individual log file paths for each sensor

### 2. Modify Each Tab Command
- Wrap existing command with bash -c structure
- Add `2>&1 | tee -a $LOG_FILE` after each primary command
- Ensure proper quoting to handle nested bash -c calls

### 3. Preserve Existing Behavior
- Keep `exec bash` at end to maintain interactive terminal
- Keep sleep delays
- Keep geometry settings
- Keep commented lines as-is

## Implementation Checklist:
1. Add timestamp generation (DATE and TIME variables) at the top of the script
2. Create LOG_BASE_DIR variable pointing to timestamped directory under /home/kodifly/logs/run_sensors/
3. Add `mkdir -p "$LOG_BASE_DIR"` to ensure directory exists
4. Define 7 log file path variables (one for each sensor process)
5. Modify Sensor Recorder tab command to include tee logging
6. Modify Distance Trigger tab command to include tee logging
7. Modify Camera tab command to include tee logging
8. Modify LiDAR tab command to include tee logging
9. Modify GPS tab command to include tee logging
10. Modify RTSP Streamer tab command to include tee logging
11. Modify Stream Forwarder tab command to include tee logging
12. Test that the script syntax is valid (bash -n check)

# Current Execution Step
> Not yet started

# Task Progress
*   2025-11-10
    *   Step: Implementation completed
    *   Modifications: 
        - Added logging infrastructure (lines 1-19)
        - Modified all 7 tab commands to include tee logging (lines 22-28)
        - Added unbuffer to 4 roslaunch tabs that suppressed output when piped (Sensor Recorder, 1m Snapshot, GPS, RTSP Streamer)
    *   Change Summary: Successfully added comprehensive logging to run_sensors script
    *   Reason: Executing implementation plan
    *   Blockers: None
    *   Status: Success

# Final Review

Implementation successfully completed. All 7 sensor processes now have:
- Real-time output display in gnome-terminal tabs
- Persistent logging to timestamped directories: /home/kodifly/logs/run_sensors/YYYYMMDD/HHMMSS/
- Both stdout and stderr captured (2>&1)
- Fixed TTY detection issue for roslaunch commands using unbuffer

Files modified:
- /home/kodifly/isds_ws/src/run_sensors (main implementation)

Solution works as intended - all tabs display output and logs are written to disk.

